import React from 'react';
import KeySetting from './keysetting/KeySetting';

const Setting = () => {
  return (
    <div>
      {/* 전체 설정
        - 음량 조절
        - 키설정 <KeySetting/>
       */}
    </div>
  );
};

export default Setting;